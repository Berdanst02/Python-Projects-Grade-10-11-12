from tkinter import *

from tkinter import messagebox

app=Tk()
app.geometry('1000x1800')
app.title("Parts Of The Computer")
app['bg']='#8a1343'

def showinput():
    pass
     #sampleLabel['text'] = "You clicked the button"
     #messagebox.showinfo("Message", "Hi there!")

def cpu():
    pass
     #sampleLabel['text'] = "You clicked the button"
     #messagebox.showinfo("Message", "Hi there!")

def cpu():
    pass
     #sampleLabel['text'] = "You clicked the button"
     #messagebox.showinfo("Message", "Hi there!"

def cpu():
    pass
     #sampleLabel['text'] = "You clicked the button"
     #messagebox.showinfo("Message", "Hi there!")
                         


inputButton = Button(text = "Input", command=showinput)
inputButton.config(font="Arial 18", width=15,bg='black',fg='white') #to change font, size, color

#sampleButton = Button(text = "Click me", command=dothis)
#sampleButton.config() #to change font, size, color

#sampleButton = Button(text = "Click me", command=dothis)
#sampleButton.config() #to change font, size, color

#sampleButton = Button(text = "Click me", command=dothis)
#sampleButton.config() #to change font, size, color

titleLabel = Label(text="Parts of the computer")
titleLabel.config(font="Arial 40",bg='black',fg='yellow',width=33) #to change font, size, color

#inputButton.place(x=10, y=10)
titleLabel.place(x=0,y=10)
inputButton.place(x=10,y=100)
 
